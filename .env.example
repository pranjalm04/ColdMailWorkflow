# Required
ANTHROPIC_API_KEY=sk-ant-...

# Apollo recruiter lookup (--apollo-company). Get a key at https://developer.apollo.io/
# APOLLO_API_KEY=...

# Optional overrides (defaults live in config.py)
# MODEL=claude-sonnet-4-6
# APOLLO_SEARCH_URL=https://api.apollo.io/api/v1/mixed_people/search
# APOLLO_MATCH_URL=https://api.apollo.io/api/v1/people/bulk_match
# DEFAULT_ROLE=Software Engineer
# CV_PATH=cv.md
# RESUME_PATH=resume/Pranjal_Mestry_resume.pdf   # attached to every email
# CONTACTS_PATH=data      # a folder (reads every .csv/.xlsx in it) or a single file
# TRACKER_PATH=output/sent_tracker.csv
# CREDENTIALS_PATH=credentials/credentials.json
# TOKEN_PATH=credentials/token.json
# SEND_DELAY_SECONDS=2.0
# MAX_SENDS_PER_RUN=50